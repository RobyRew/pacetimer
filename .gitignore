# macOS
.DS_Store

# Xcode / build output
build/
DerivedData/
*.xcodeproj          # generated by XcodeGen (`xcodegen generate`)
*.xcworkspace
xcuserdata/
*.xcuserstate
.swiftpm/

# Local release artifacts (CI produces these on the runner)
*.zip
*.dmg

# Intermediate iconset (AppIcon.icns is the committed artifact)
Packaging/AppIcon.iconset/
